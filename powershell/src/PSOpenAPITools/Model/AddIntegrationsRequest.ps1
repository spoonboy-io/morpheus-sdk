#
# Morpheus API
# Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
# Version: 6.2.1
# Contact: dev@morpheusdata.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

AddIntegrationsRequest<PSCustomObject>
#>
function ConvertFrom-JsonToAddIntegrationsRequest {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match IntegrationAnsibleConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIntegrationAnsibleConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IntegrationAnsibleConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IntegrationAnsibleConfig' defined in oneOf (AddIntegrationsRequest). Proceeding to the next one if any."
        }

        # try to match IntegrationConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIntegrationConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IntegrationConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IntegrationConfig' defined in oneOf (AddIntegrationsRequest). Proceeding to the next one if any."
        }

        # try to match IntegrationDockerRepoConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIntegrationDockerRepoConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IntegrationDockerRepoConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IntegrationDockerRepoConfig' defined in oneOf (AddIntegrationsRequest). Proceeding to the next one if any."
        }

        # try to match IntegrationGitHubConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIntegrationGitHubConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IntegrationGitHubConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IntegrationGitHubConfig' defined in oneOf (AddIntegrationsRequest). Proceeding to the next one if any."
        }

        # try to match IntegrationGitRepoConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIntegrationGitRepoConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IntegrationGitRepoConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IntegrationGitRepoConfig' defined in oneOf (AddIntegrationsRequest). Proceeding to the next one if any."
        }

        # try to match IntegrationSNOWConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIntegrationSNOWConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IntegrationSNOWConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IntegrationSNOWConfig' defined in oneOf (AddIntegrationsRequest). Proceeding to the next one if any."
        }

        # try to match IntegrationSaltMasterConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIntegrationSaltMasterConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IntegrationSaltMasterConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IntegrationSaltMasterConfig' defined in oneOf (AddIntegrationsRequest). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([IntegrationAnsibleConfig, IntegrationConfig, IntegrationDockerRepoConfig, IntegrationGitHubConfig, IntegrationGitRepoConfig, IntegrationSNOWConfig, IntegrationSaltMasterConfig]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("IntegrationAnsibleConfig", "IntegrationConfig", "IntegrationDockerRepoConfig", "IntegrationGitHubConfig", "IntegrationGitRepoConfig", "IntegrationSNOWConfig", "IntegrationSaltMasterConfig")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([IntegrationAnsibleConfig, IntegrationConfig, IntegrationDockerRepoConfig, IntegrationGitHubConfig, IntegrationGitRepoConfig, IntegrationSNOWConfig, IntegrationSaltMasterConfig]). JSON Payload: $($Json)"
        }
    }
}

