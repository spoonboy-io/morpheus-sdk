#
# Morpheus API
# Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
# Version: 6.2.1
# Contact: dev@morpheusdata.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

AddBackupsRequestBackup<PSCustomObject>
#>
function ConvertFrom-JsonToAddBackupsRequestBackup {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        if ($match -ne 0) { # no match yet
            # try to match BackupTypeInstance defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToBackupTypeInstance $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "BackupTypeInstance"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'BackupTypeInstance' defined in anyOf (AddBackupsRequestBackup). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match BackupTypeProvider defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToBackupTypeProvider $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "BackupTypeProvider"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'BackupTypeProvider' defined in anyOf (AddBackupsRequestBackup). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match BackupTypeServer defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToBackupTypeServer $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "BackupTypeServer"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'BackupTypeServer' defined in anyOf (AddBackupsRequestBackup). Proceeding to the next one if any."
            }
        }

        if ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "anyOfSchemas" = @("BackupTypeInstance", "BackupTypeProvider", "BackupTypeServer")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in anyOf schemas ([BackupTypeInstance, BackupTypeProvider, BackupTypeServer]). JSON Payload: $($Json)"
        }
    }
}

