#
# Morpheus API
# Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
# Version: 6.2.1
# Contact: dev@morpheusdata.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

CheckConfig<PSCustomObject>
#>
function ConvertFrom-JsonToCheckConfig {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        if ($match -ne 0) { # no match yet
            # try to match CheckElasticsearchConfig defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToCheckElasticsearchConfig $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "CheckElasticsearchConfig"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'CheckElasticsearchConfig' defined in anyOf (CheckConfig). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match CheckSocketConfig defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToCheckSocketConfig $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "CheckSocketConfig"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'CheckSocketConfig' defined in anyOf (CheckConfig). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match CheckSqlConfig defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToCheckSqlConfig $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "CheckSqlConfig"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'CheckSqlConfig' defined in anyOf (CheckConfig). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match CheckVmConfig defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToCheckVmConfig $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "CheckVmConfig"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'CheckVmConfig' defined in anyOf (CheckConfig). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match CheckWebConfig defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToCheckWebConfig $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "CheckWebConfig"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'CheckWebConfig' defined in anyOf (CheckConfig). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match SystemCollectionsHashtable defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToSystemCollectionsHashtable $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "SystemCollectionsHashtable"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'SystemCollectionsHashtable' defined in anyOf (CheckConfig). Proceeding to the next one if any."
            }
        }

        if ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "anyOfSchemas" = @("CheckElasticsearchConfig", "CheckSocketConfig", "CheckSqlConfig", "CheckVmConfig", "CheckWebConfig", "SystemCollectionsHashtable")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in anyOf schemas ([CheckElasticsearchConfig, CheckSocketConfig, CheckSqlConfig, CheckVmConfig, CheckWebConfig, SystemCollectionsHashtable]). JSON Payload: $($Json)"
        }
    }
}

