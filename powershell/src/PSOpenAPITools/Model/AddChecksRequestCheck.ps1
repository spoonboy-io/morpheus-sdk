#
# Morpheus API
# Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
# Version: 6.2.1
# Contact: dev@morpheusdata.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

Payload for creating a new monitoring check

.PARAMETER Json

JSON object

.OUTPUTS

AddChecksRequestCheck<PSCustomObject>
#>
function ConvertFrom-JsonToAddChecksRequestCheck {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match CheckElastic defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCheckElastic $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CheckElastic"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CheckElastic' defined in oneOf (AddChecksRequestCheck). Proceeding to the next one if any."
        }

        # try to match CheckPush defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCheckPush $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CheckPush"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CheckPush' defined in oneOf (AddChecksRequestCheck). Proceeding to the next one if any."
        }

        # try to match CheckSocket defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCheckSocket $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CheckSocket"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CheckSocket' defined in oneOf (AddChecksRequestCheck). Proceeding to the next one if any."
        }

        # try to match CheckSql defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCheckSql $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CheckSql"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CheckSql' defined in oneOf (AddChecksRequestCheck). Proceeding to the next one if any."
        }

        # try to match CheckWeb defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCheckWeb $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CheckWeb"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CheckWeb' defined in oneOf (AddChecksRequestCheck). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([CheckElastic, CheckPush, CheckSocket, CheckSql, CheckWeb]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("CheckElastic", "CheckPush", "CheckSocket", "CheckSql", "CheckWeb")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([CheckElastic, CheckPush, CheckSocket, CheckSql, CheckWeb]). JSON Payload: $($Json)"
        }
    }
}

