#
# Morpheus API
# Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
# Version: 6.2.1
# Contact: dev@morpheusdata.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

Payload for creating a new credential

.PARAMETER Json

JSON object

.OUTPUTS

AddCredentialsRequestCredential<PSCustomObject>
#>
function ConvertFrom-JsonToAddCredentialsRequestCredential {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match CredentialAccessSecretKeyConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCredentialAccessSecretKeyConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CredentialAccessSecretKeyConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CredentialAccessSecretKeyConfig' defined in oneOf (AddCredentialsRequestCredential). Proceeding to the next one if any."
        }

        # try to match CredentialClientIDSecretConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCredentialClientIDSecretConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CredentialClientIDSecretConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CredentialClientIDSecretConfig' defined in oneOf (AddCredentialsRequestCredential). Proceeding to the next one if any."
        }

        # try to match CredentialEmailPrivateKeyConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCredentialEmailPrivateKeyConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CredentialEmailPrivateKeyConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CredentialEmailPrivateKeyConfig' defined in oneOf (AddCredentialsRequestCredential). Proceeding to the next one if any."
        }

        # try to match CredentialOauth2Config defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCredentialOauth2Config $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CredentialOauth2Config"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CredentialOauth2Config' defined in oneOf (AddCredentialsRequestCredential). Proceeding to the next one if any."
        }

        # try to match CredentialTenantUsernameKeypairConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCredentialTenantUsernameKeypairConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CredentialTenantUsernameKeypairConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CredentialTenantUsernameKeypairConfig' defined in oneOf (AddCredentialsRequestCredential). Proceeding to the next one if any."
        }

        # try to match CredentialUsernameAPIKeyConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCredentialUsernameAPIKeyConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CredentialUsernameAPIKeyConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CredentialUsernameAPIKeyConfig' defined in oneOf (AddCredentialsRequestCredential). Proceeding to the next one if any."
        }

        # try to match CredentialUsernameKeypairConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCredentialUsernameKeypairConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CredentialUsernameKeypairConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CredentialUsernameKeypairConfig' defined in oneOf (AddCredentialsRequestCredential). Proceeding to the next one if any."
        }

        # try to match CredentialUsernamePasswordConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCredentialUsernamePasswordConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CredentialUsernamePasswordConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CredentialUsernamePasswordConfig' defined in oneOf (AddCredentialsRequestCredential). Proceeding to the next one if any."
        }

        # try to match CredentialUsernamePasswordKeypairConfig defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCredentialUsernamePasswordKeypairConfig $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CredentialUsernamePasswordKeypairConfig"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CredentialUsernamePasswordKeypairConfig' defined in oneOf (AddCredentialsRequestCredential). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([CredentialAccessSecretKeyConfig, CredentialClientIDSecretConfig, CredentialEmailPrivateKeyConfig, CredentialOauth2Config, CredentialTenantUsernameKeypairConfig, CredentialUsernameAPIKeyConfig, CredentialUsernameKeypairConfig, CredentialUsernamePasswordConfig, CredentialUsernamePasswordKeypairConfig]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("CredentialAccessSecretKeyConfig", "CredentialClientIDSecretConfig", "CredentialEmailPrivateKeyConfig", "CredentialOauth2Config", "CredentialTenantUsernameKeypairConfig", "CredentialUsernameAPIKeyConfig", "CredentialUsernameKeypairConfig", "CredentialUsernamePasswordConfig", "CredentialUsernamePasswordKeypairConfig")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([CredentialAccessSecretKeyConfig, CredentialClientIDSecretConfig, CredentialEmailPrivateKeyConfig, CredentialOauth2Config, CredentialTenantUsernameKeypairConfig, CredentialUsernameAPIKeyConfig, CredentialUsernameKeypairConfig, CredentialUsernamePasswordConfig, CredentialUsernamePasswordKeypairConfig]). JSON Payload: $($Json)"
        }
    }
}

