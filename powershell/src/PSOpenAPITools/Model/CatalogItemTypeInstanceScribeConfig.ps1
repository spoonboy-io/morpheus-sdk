#
# Morpheus API
# Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
# Version: 6.2.1
# Contact: dev@morpheusdata.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

CatalogItemTypeInstanceScribeConfig<PSCustomObject>
#>
function ConvertFrom-JsonToCatalogItemTypeInstanceScribeConfig {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        if ($match -ne 0) { # no match yet
            # try to match InstancesConfigAWS defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToInstancesConfigAWS $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "InstancesConfigAWS"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'InstancesConfigAWS' defined in anyOf (CatalogItemTypeInstanceScribeConfig). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match InstancesConfigAzure defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToInstancesConfigAzure $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "InstancesConfigAzure"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'InstancesConfigAzure' defined in anyOf (CatalogItemTypeInstanceScribeConfig). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match InstancesConfigGCP defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToInstancesConfigGCP $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "InstancesConfigGCP"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'InstancesConfigGCP' defined in anyOf (CatalogItemTypeInstanceScribeConfig). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match InstancesConfigVMWare defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToInstancesConfigVMWare $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "InstancesConfigVMWare"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'InstancesConfigVMWare' defined in anyOf (CatalogItemTypeInstanceScribeConfig). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match SystemCollectionsHashtable defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToSystemCollectionsHashtable $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "SystemCollectionsHashtable"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'SystemCollectionsHashtable' defined in anyOf (CatalogItemTypeInstanceScribeConfig). Proceeding to the next one if any."
            }
        }

        if ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "anyOfSchemas" = @("InstancesConfigAWS", "InstancesConfigAzure", "InstancesConfigGCP", "InstancesConfigVMWare", "SystemCollectionsHashtable")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in anyOf schemas ([InstancesConfigAWS, InstancesConfigAzure, InstancesConfigGCP, InstancesConfigVMWare, SystemCollectionsHashtable]). JSON Payload: $($Json)"
        }
    }
}

