/**
 * Morpheus API
 * Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
 *
 * The version of the OpenAPI document: 6.1.1
 * Contact: dev@morpheusdata.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BlueprintARMCreateArm from './BlueprintARMCreateArm';

/**
 * The BlueprintARMCreate model module.
 * @module model/BlueprintARMCreate
 * @version 6.1.1
 */
class BlueprintARMCreate {
    /**
     * Constructs a new <code>BlueprintARMCreate</code>.
     * @alias module:model/BlueprintARMCreate
     * @param name {String} A name for the blueprint
     * @param type {module:model/BlueprintARMCreate.TypeEnum} Blueprint Type
     * @param arm {module:model/BlueprintARMCreateArm} 
     */
    constructor(name, type, arm) { 
        
        BlueprintARMCreate.initialize(this, name, type, arm);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, type, arm) { 
        obj['name'] = name;
        obj['type'] = type;
        obj['arm'] = arm;
    }

    /**
     * Constructs a <code>BlueprintARMCreate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BlueprintARMCreate} obj Optional instance to populate.
     * @return {module:model/BlueprintARMCreate} The populated <code>BlueprintARMCreate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BlueprintARMCreate();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('image')) {
                obj['image'] = ApiClient.convertToType(data['image'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], ['String']);
            }
            if (data.hasOwnProperty('arm')) {
                obj['arm'] = BlueprintARMCreateArm.constructFromObject(data['arm']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BlueprintARMCreate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BlueprintARMCreate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BlueprintARMCreate.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['image'] && !(typeof data['image'] === 'string' || data['image'] instanceof String)) {
            throw new Error("Expected the field `image` to be a primitive type in the JSON string but got " + data['image']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['labels'])) {
            throw new Error("Expected the field `labels` to be an array in the JSON data but got " + data['labels']);
        }
        // validate the optional field `arm`
        if (data['arm']) { // data not null
          BlueprintARMCreateArm.validateJSON(data['arm']);
        }

        return true;
    }


}

BlueprintARMCreate.RequiredProperties = ["name", "type", "arm"];

/**
 * A name for the blueprint
 * @member {String} name
 */
BlueprintARMCreate.prototype['name'] = undefined;

/**
 * Path to display image. Defaults to an internal Morpheus image.
 * @member {String} image
 */
BlueprintARMCreate.prototype['image'] = undefined;

/**
 * Blueprint Type
 * @member {module:model/BlueprintARMCreate.TypeEnum} type
 */
BlueprintARMCreate.prototype['type'] = undefined;

/**
 * Array of label strings, can be used for filtering.
 * @member {Array.<String>} labels
 */
BlueprintARMCreate.prototype['labels'] = undefined;

/**
 * @member {module:model/BlueprintARMCreateArm} arm
 */
BlueprintARMCreate.prototype['arm'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
BlueprintARMCreate['TypeEnum'] = {

    /**
     * value: "arm"
     * @const
     */
    "arm": "arm"
};



export default BlueprintARMCreate;

