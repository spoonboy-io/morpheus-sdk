/**
 * Morpheus API
 * Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
 *
 * The version of the OpenAPI document: 6.2.1
 * Contact: dev@morpheusdata.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CheckPushCheckType from './CheckPushCheckType';

/**
 * The CheckPush model module.
 * @module model/CheckPush
 * @version 6.2.1
 */
class CheckPush {
    /**
     * Constructs a new <code>CheckPush</code>.
     * A push Check is not polled regularly by the standard monitoring system. Instead it is expected that an external API push updates as to the status of the check timed closely with the configured check interval setting. This is used to throttle the push from performing too many status updates. To push an update using the api key one must send a json payload like so: &#x60;curl -XPOST https://&lt;morpheus url&gt;/api/monitoring/push?apiKey&#x3D;&lt;apiKey&gt; -H &#39;Content-Type: application/json&#39; -d &#39;{\&quot;success\&quot;:true, \&quot;message\&quot;: \&quot;any comment goes here\&quot;}&#39;&#x60; The API Key will be returned on successful creation or can be found by getting this check. 
     * @alias module:model/CheckPush
     */
    constructor() { 
        
        CheckPush.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CheckPush</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CheckPush} obj Optional instance to populate.
     * @return {module:model/CheckPush} The populated <code>CheckPush</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CheckPush();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('checkType')) {
                obj['checkType'] = CheckPushCheckType.constructFromObject(data['checkType']);
            }
            if (data.hasOwnProperty('checkInterval')) {
                obj['checkInterval'] = ApiClient.convertToType(data['checkInterval'], 'Number');
            }
            if (data.hasOwnProperty('inUptime')) {
                obj['inUptime'] = ApiClient.convertToType(data['inUptime'], 'Boolean');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('severity')) {
                obj['severity'] = ApiClient.convertToType(data['severity'], 'String');
            }
        }
        return obj;
    }


}

/**
 * Unique name scoped to your account for the check
 * @member {String} name
 */
CheckPush.prototype['name'] = undefined;

/**
 * Optional description field
 * @member {String} description
 */
CheckPush.prototype['description'] = undefined;

/**
 * @member {module:model/CheckPushCheckType} checkType
 */
CheckPush.prototype['checkType'] = undefined;

/**
 * Number of seconds you want between check executions (minimum value is 60, depending on your subscription plan)
 * @member {Number} checkInterval
 * @default 300
 */
CheckPush.prototype['checkInterval'] = 300;

/**
 * Used to determine if check should affect account wide availability calculations
 * @member {Boolean} inUptime
 * @default true
 */
CheckPush.prototype['inUptime'] = true;

/**
 * Used to determine if check should be scheduled to execute
 * @member {Boolean} active
 * @default true
 */
CheckPush.prototype['active'] = true;

/**
 * Severity level threshold for sending notifications.
 * @member {module:model/CheckPush.SeverityEnum} severity
 * @default 'critical'
 */
CheckPush.prototype['severity'] = 'critical';





/**
 * Allowed values for the <code>severity</code> property.
 * @enum {String}
 * @readonly
 */
CheckPush['SeverityEnum'] = {

    /**
     * value: "info"
     * @const
     */
    "info": "info",

    /**
     * value: "warning"
     * @const
     */
    "warning": "warning",

    /**
     * value: "critical"
     * @const
     */
    "critical": "critical"
};



export default CheckPush;

