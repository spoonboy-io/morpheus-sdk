/**
 * Morpheus API
 * Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
 *
 * The version of the OpenAPI document: 6.1.1
 * Contact: dev@morpheusdata.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UpdateExecuteSchedulesRequestSchedule model module.
 * @module model/UpdateExecuteSchedulesRequestSchedule
 * @version 6.1.1
 */
class UpdateExecuteSchedulesRequestSchedule {
    /**
     * Constructs a new <code>UpdateExecuteSchedulesRequestSchedule</code>.
     * @alias module:model/UpdateExecuteSchedulesRequestSchedule
     */
    constructor() { 
        
        UpdateExecuteSchedulesRequestSchedule.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>UpdateExecuteSchedulesRequestSchedule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateExecuteSchedulesRequestSchedule} obj Optional instance to populate.
     * @return {module:model/UpdateExecuteSchedulesRequestSchedule} The populated <code>UpdateExecuteSchedulesRequestSchedule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateExecuteSchedulesRequestSchedule();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('scheduleType')) {
                obj['scheduleType'] = ApiClient.convertToType(data['scheduleType'], 'String');
            }
            if (data.hasOwnProperty('scheduleTimezone')) {
                obj['scheduleTimezone'] = ApiClient.convertToType(data['scheduleTimezone'], 'String');
            }
            if (data.hasOwnProperty('cron')) {
                obj['cron'] = ApiClient.convertToType(data['cron'], 'String');
            }
            if (data.hasOwnProperty('enabled')) {
                obj['enabled'] = ApiClient.convertToType(data['enabled'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateExecuteSchedulesRequestSchedule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateExecuteSchedulesRequestSchedule</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['scheduleType'] && !(typeof data['scheduleType'] === 'string' || data['scheduleType'] instanceof String)) {
            throw new Error("Expected the field `scheduleType` to be a primitive type in the JSON string but got " + data['scheduleType']);
        }
        // ensure the json data is a string
        if (data['scheduleTimezone'] && !(typeof data['scheduleTimezone'] === 'string' || data['scheduleTimezone'] instanceof String)) {
            throw new Error("Expected the field `scheduleTimezone` to be a primitive type in the JSON string but got " + data['scheduleTimezone']);
        }
        // ensure the json data is a string
        if (data['cron'] && !(typeof data['cron'] === 'string' || data['cron'] instanceof String)) {
            throw new Error("Expected the field `cron` to be a primitive type in the JSON string but got " + data['cron']);
        }

        return true;
    }


}



/**
 * A name for the execute schedule
 * @member {String} name
 */
UpdateExecuteSchedulesRequestSchedule.prototype['name'] = undefined;

/**
 * A description for the execute schedule
 * @member {String} description
 */
UpdateExecuteSchedulesRequestSchedule.prototype['description'] = undefined;

/**
 * Type of schedule
 * @member {module:model/UpdateExecuteSchedulesRequestSchedule.ScheduleTypeEnum} scheduleType
 */
UpdateExecuteSchedulesRequestSchedule.prototype['scheduleType'] = undefined;

/**
 * Time Zone eg. America/New_York, Europe/Amsterdam, etc.
 * @member {String} scheduleTimezone
 * @default 'UTC'
 */
UpdateExecuteSchedulesRequestSchedule.prototype['scheduleTimezone'] = 'UTC';

/**
 * Cron Expression. The default is daily at midnight
 * @member {String} cron
 * @default '0 0 * * *'
 */
UpdateExecuteSchedulesRequestSchedule.prototype['cron'] = '0 0 * * *';

/**
 * Is enabled
 * @member {Boolean} enabled
 * @default true
 */
UpdateExecuteSchedulesRequestSchedule.prototype['enabled'] = true;





/**
 * Allowed values for the <code>scheduleType</code> property.
 * @enum {String}
 * @readonly
 */
UpdateExecuteSchedulesRequestSchedule['ScheduleTypeEnum'] = {

    /**
     * value: "execute"
     * @const
     */
    "execute": "execute"
};



export default UpdateExecuteSchedulesRequestSchedule;

