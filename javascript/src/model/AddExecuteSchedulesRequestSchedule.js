/**
 * Morpheus API
 * Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
 *
 * The version of the OpenAPI document: 6.1.1
 * Contact: dev@morpheusdata.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AddExecuteSchedulesRequestSchedule model module.
 * @module model/AddExecuteSchedulesRequestSchedule
 * @version 6.1.1
 */
class AddExecuteSchedulesRequestSchedule {
    /**
     * Constructs a new <code>AddExecuteSchedulesRequestSchedule</code>.
     * @alias module:model/AddExecuteSchedulesRequestSchedule
     * @param name {String} A name for the execute schedule
     */
    constructor(name) { 
        
        AddExecuteSchedulesRequestSchedule.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>AddExecuteSchedulesRequestSchedule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AddExecuteSchedulesRequestSchedule} obj Optional instance to populate.
     * @return {module:model/AddExecuteSchedulesRequestSchedule} The populated <code>AddExecuteSchedulesRequestSchedule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AddExecuteSchedulesRequestSchedule();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('scheduleType')) {
                obj['scheduleType'] = ApiClient.convertToType(data['scheduleType'], 'String');
            }
            if (data.hasOwnProperty('scheduleTimezone')) {
                obj['scheduleTimezone'] = ApiClient.convertToType(data['scheduleTimezone'], 'String');
            }
            if (data.hasOwnProperty('cron')) {
                obj['cron'] = ApiClient.convertToType(data['cron'], 'String');
            }
            if (data.hasOwnProperty('enabled')) {
                obj['enabled'] = ApiClient.convertToType(data['enabled'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AddExecuteSchedulesRequestSchedule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AddExecuteSchedulesRequestSchedule</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AddExecuteSchedulesRequestSchedule.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['scheduleType'] && !(typeof data['scheduleType'] === 'string' || data['scheduleType'] instanceof String)) {
            throw new Error("Expected the field `scheduleType` to be a primitive type in the JSON string but got " + data['scheduleType']);
        }
        // ensure the json data is a string
        if (data['scheduleTimezone'] && !(typeof data['scheduleTimezone'] === 'string' || data['scheduleTimezone'] instanceof String)) {
            throw new Error("Expected the field `scheduleTimezone` to be a primitive type in the JSON string but got " + data['scheduleTimezone']);
        }
        // ensure the json data is a string
        if (data['cron'] && !(typeof data['cron'] === 'string' || data['cron'] instanceof String)) {
            throw new Error("Expected the field `cron` to be a primitive type in the JSON string but got " + data['cron']);
        }

        return true;
    }


}

AddExecuteSchedulesRequestSchedule.RequiredProperties = ["name"];

/**
 * A name for the execute schedule
 * @member {String} name
 */
AddExecuteSchedulesRequestSchedule.prototype['name'] = undefined;

/**
 * A description for the execute schedule
 * @member {String} description
 */
AddExecuteSchedulesRequestSchedule.prototype['description'] = undefined;

/**
 * Type of schedule
 * @member {module:model/AddExecuteSchedulesRequestSchedule.ScheduleTypeEnum} scheduleType
 */
AddExecuteSchedulesRequestSchedule.prototype['scheduleType'] = undefined;

/**
 * Time Zone eg. America/New_York, Europe/Amsterdam, etc.
 * @member {String} scheduleTimezone
 * @default 'UTC'
 */
AddExecuteSchedulesRequestSchedule.prototype['scheduleTimezone'] = 'UTC';

/**
 * Cron Expression. The default is daily at midnight
 * @member {String} cron
 * @default '0 0 * * *'
 */
AddExecuteSchedulesRequestSchedule.prototype['cron'] = '0 0 * * *';

/**
 * Is enabled
 * @member {Boolean} enabled
 * @default true
 */
AddExecuteSchedulesRequestSchedule.prototype['enabled'] = true;





/**
 * Allowed values for the <code>scheduleType</code> property.
 * @enum {String}
 * @readonly
 */
AddExecuteSchedulesRequestSchedule['ScheduleTypeEnum'] = {

    /**
     * value: "execute"
     * @const
     */
    "execute": "execute"
};



export default AddExecuteSchedulesRequestSchedule;

