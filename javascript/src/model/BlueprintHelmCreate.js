/**
 * Morpheus API
 * Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
 *
 * The version of the OpenAPI document: 6.1.1
 * Contact: dev@morpheusdata.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BlueprintHelmCreateHelm from './BlueprintHelmCreateHelm';

/**
 * The BlueprintHelmCreate model module.
 * @module model/BlueprintHelmCreate
 * @version 6.1.1
 */
class BlueprintHelmCreate {
    /**
     * Constructs a new <code>BlueprintHelmCreate</code>.
     * @alias module:model/BlueprintHelmCreate
     * @param name {String} A name for the blueprint
     * @param type {module:model/BlueprintHelmCreate.TypeEnum} Blueprint Type
     * @param helm {module:model/BlueprintHelmCreateHelm} 
     */
    constructor(name, type, helm) { 
        
        BlueprintHelmCreate.initialize(this, name, type, helm);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, type, helm) { 
        obj['name'] = name;
        obj['type'] = type;
        obj['helm'] = helm;
    }

    /**
     * Constructs a <code>BlueprintHelmCreate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BlueprintHelmCreate} obj Optional instance to populate.
     * @return {module:model/BlueprintHelmCreate} The populated <code>BlueprintHelmCreate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BlueprintHelmCreate();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('image')) {
                obj['image'] = ApiClient.convertToType(data['image'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], ['String']);
            }
            if (data.hasOwnProperty('helm')) {
                obj['helm'] = BlueprintHelmCreateHelm.constructFromObject(data['helm']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BlueprintHelmCreate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BlueprintHelmCreate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BlueprintHelmCreate.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['image'] && !(typeof data['image'] === 'string' || data['image'] instanceof String)) {
            throw new Error("Expected the field `image` to be a primitive type in the JSON string but got " + data['image']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['labels'])) {
            throw new Error("Expected the field `labels` to be an array in the JSON data but got " + data['labels']);
        }
        // validate the optional field `helm`
        if (data['helm']) { // data not null
          BlueprintHelmCreateHelm.validateJSON(data['helm']);
        }

        return true;
    }


}

BlueprintHelmCreate.RequiredProperties = ["name", "type", "helm"];

/**
 * A name for the blueprint
 * @member {String} name
 */
BlueprintHelmCreate.prototype['name'] = undefined;

/**
 * Path to display image. Defaults to an internal Morpheus image.
 * @member {String} image
 */
BlueprintHelmCreate.prototype['image'] = undefined;

/**
 * Blueprint Type
 * @member {module:model/BlueprintHelmCreate.TypeEnum} type
 */
BlueprintHelmCreate.prototype['type'] = undefined;

/**
 * Array of label strings, can be used for filtering.
 * @member {Array.<String>} labels
 */
BlueprintHelmCreate.prototype['labels'] = undefined;

/**
 * @member {module:model/BlueprintHelmCreateHelm} helm
 */
BlueprintHelmCreate.prototype['helm'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
BlueprintHelmCreate['TypeEnum'] = {

    /**
     * value: "helm"
     * @const
     */
    "helm": "helm"
};



export default BlueprintHelmCreate;

