/**
 * Morpheus API
 * Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
 *
 * The version of the OpenAPI document: 6.1.1
 * Contact: dev@morpheusdata.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AddCheckAppsRequestMonitorApp model module.
 * @module model/AddCheckAppsRequestMonitorApp
 * @version 6.1.1
 */
class AddCheckAppsRequestMonitorApp {
    /**
     * Constructs a new <code>AddCheckAppsRequestMonitorApp</code>.
     * Payload for creating a new monitoring check app
     * @alias module:model/AddCheckAppsRequestMonitorApp
     * @param name {String} Unique name scoped to your account for the check app
     */
    constructor(name) { 
        
        AddCheckAppsRequestMonitorApp.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>AddCheckAppsRequestMonitorApp</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AddCheckAppsRequestMonitorApp} obj Optional instance to populate.
     * @return {module:model/AddCheckAppsRequestMonitorApp} The populated <code>AddCheckAppsRequestMonitorApp</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AddCheckAppsRequestMonitorApp();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('inUptime')) {
                obj['inUptime'] = ApiClient.convertToType(data['inUptime'], 'Boolean');
            }
            if (data.hasOwnProperty('severity')) {
                obj['severity'] = ApiClient.convertToType(data['severity'], 'String');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('checks')) {
                obj['checks'] = ApiClient.convertToType(data['checks'], ['Number']);
            }
            if (data.hasOwnProperty('checkGroups')) {
                obj['checkGroups'] = ApiClient.convertToType(data['checkGroups'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AddCheckAppsRequestMonitorApp</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AddCheckAppsRequestMonitorApp</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AddCheckAppsRequestMonitorApp.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['severity'] && !(typeof data['severity'] === 'string' || data['severity'] instanceof String)) {
            throw new Error("Expected the field `severity` to be a primitive type in the JSON string but got " + data['severity']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['checks'])) {
            throw new Error("Expected the field `checks` to be an array in the JSON data but got " + data['checks']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['checkGroups'])) {
            throw new Error("Expected the field `checkGroups` to be an array in the JSON data but got " + data['checkGroups']);
        }

        return true;
    }


}

AddCheckAppsRequestMonitorApp.RequiredProperties = ["name"];

/**
 * Unique name scoped to your account for the check app
 * @member {String} name
 */
AddCheckAppsRequestMonitorApp.prototype['name'] = undefined;

/**
 * Optional description field
 * @member {String} description
 */
AddCheckAppsRequestMonitorApp.prototype['description'] = undefined;

/**
 * Used to determine if check should affect account wide availability calculations
 * @member {Boolean} inUptime
 * @default true
 */
AddCheckAppsRequestMonitorApp.prototype['inUptime'] = true;

/**
 * Severity level of incidents that are created when this check fails
 * @member {module:model/AddCheckAppsRequestMonitorApp.SeverityEnum} severity
 * @default 'critical'
 */
AddCheckAppsRequestMonitorApp.prototype['severity'] = 'critical';

/**
 * Used to determine if check app is active
 * @member {Boolean} active
 * @default true
 */
AddCheckAppsRequestMonitorApp.prototype['active'] = true;

/**
 * @member {Array.<Number>} checks
 */
AddCheckAppsRequestMonitorApp.prototype['checks'] = undefined;

/**
 * @member {Array.<Number>} checkGroups
 */
AddCheckAppsRequestMonitorApp.prototype['checkGroups'] = undefined;





/**
 * Allowed values for the <code>severity</code> property.
 * @enum {String}
 * @readonly
 */
AddCheckAppsRequestMonitorApp['SeverityEnum'] = {

    /**
     * value: "info"
     * @const
     */
    "info": "info",

    /**
     * value: "warning"
     * @const
     */
    "warning": "warning",

    /**
     * value: "critical"
     * @const
     */
    "critical": "critical"
};



export default AddCheckAppsRequestMonitorApp;

