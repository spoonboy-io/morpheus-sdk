/**
 * Morpheus API
 * Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
 *
 * The version of the OpenAPI document: 6.2.1
 * Contact: dev@morpheusdata.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CheckSocketCheckType from './CheckSocketCheckType';
import CheckSocketConfig from './CheckSocketConfig';

/**
 * The CheckSocket model module.
 * @module model/CheckSocket
 * @version 6.2.1
 */
class CheckSocket {
    /**
     * Constructs a new <code>CheckSocket</code>.
     * Socket check confirms a certain TCP port is up and responding in your environment.  It can be configured do an initial send upon connect and compare and expected response of the service. 
     * @alias module:model/CheckSocket
     */
    constructor() { 
        
        CheckSocket.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CheckSocket</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CheckSocket} obj Optional instance to populate.
     * @return {module:model/CheckSocket} The populated <code>CheckSocket</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CheckSocket();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('checkType')) {
                obj['checkType'] = CheckSocketCheckType.constructFromObject(data['checkType']);
            }
            if (data.hasOwnProperty('checkInterval')) {
                obj['checkInterval'] = ApiClient.convertToType(data['checkInterval'], 'Number');
            }
            if (data.hasOwnProperty('inUptime')) {
                obj['inUptime'] = ApiClient.convertToType(data['inUptime'], 'Boolean');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('severity')) {
                obj['severity'] = ApiClient.convertToType(data['severity'], 'String');
            }
            if (data.hasOwnProperty('config')) {
                obj['config'] = ApiClient.convertToType(data['config'], CheckSocketConfig);
            }
        }
        return obj;
    }


}

/**
 * Unique name scoped to your account for the check
 * @member {String} name
 */
CheckSocket.prototype['name'] = undefined;

/**
 * Optional description field
 * @member {String} description
 */
CheckSocket.prototype['description'] = undefined;

/**
 * @member {module:model/CheckSocketCheckType} checkType
 */
CheckSocket.prototype['checkType'] = undefined;

/**
 * Number of seconds you want between check executions (minimum value is 60, depending on your subscription plan)
 * @member {Number} checkInterval
 * @default 300
 */
CheckSocket.prototype['checkInterval'] = 300;

/**
 * Used to determine if check should affect account wide availability calculations
 * @member {Boolean} inUptime
 * @default true
 */
CheckSocket.prototype['inUptime'] = true;

/**
 * Used to determine if check should be scheduled to execute
 * @member {Boolean} active
 * @default true
 */
CheckSocket.prototype['active'] = true;

/**
 * Severity level threshold for sending notifications.
 * @member {module:model/CheckSocket.SeverityEnum} severity
 * @default 'critical'
 */
CheckSocket.prototype['severity'] = 'critical';

/**
 * @member {module:model/CheckSocketConfig} config
 */
CheckSocket.prototype['config'] = undefined;





/**
 * Allowed values for the <code>severity</code> property.
 * @enum {String}
 * @readonly
 */
CheckSocket['SeverityEnum'] = {

    /**
     * value: "info"
     * @const
     */
    "info": "info",

    /**
     * value: "warning"
     * @const
     */
    "warning": "warning",

    /**
     * value: "critical"
     * @const
     */
    "critical": "critical"
};



export default CheckSocket;

