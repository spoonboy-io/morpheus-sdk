/**
 * Morpheus API
 * Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
 *
 * The version of the OpenAPI document: 6.1.1
 * Contact: dev@morpheusdata.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Alert from './Alert';
import Check from './Check';
import CheckApp from './CheckApp';
import CheckGroup from './CheckGroup';

/**
 * The GetAlerts200Response model module.
 * @module model/GetAlerts200Response
 * @version 6.1.1
 */
class GetAlerts200Response {
    /**
     * Constructs a new <code>GetAlerts200Response</code>.
     * @alias module:model/GetAlerts200Response
     */
    constructor() { 
        
        GetAlerts200Response.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GetAlerts200Response</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetAlerts200Response} obj Optional instance to populate.
     * @return {module:model/GetAlerts200Response} The populated <code>GetAlerts200Response</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetAlerts200Response();

            if (data.hasOwnProperty('alert')) {
                obj['alert'] = Alert.constructFromObject(data['alert']);
            }
            if (data.hasOwnProperty('checks')) {
                obj['checks'] = ApiClient.convertToType(data['checks'], [Check]);
            }
            if (data.hasOwnProperty('checkGroups')) {
                obj['checkGroups'] = ApiClient.convertToType(data['checkGroups'], [CheckGroup]);
            }
            if (data.hasOwnProperty('apps')) {
                obj['apps'] = ApiClient.convertToType(data['apps'], [CheckApp]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetAlerts200Response</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetAlerts200Response</code>.
     */
    static validateJSON(data) {
        // validate the optional field `alert`
        if (data['alert']) { // data not null
          Alert.validateJSON(data['alert']);
        }
        if (data['checks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['checks'])) {
                throw new Error("Expected the field `checks` to be an array in the JSON data but got " + data['checks']);
            }
            // validate the optional field `checks` (array)
            for (const item of data['checks']) {
                Check.validateJSON(item);
            };
        }
        if (data['checkGroups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['checkGroups'])) {
                throw new Error("Expected the field `checkGroups` to be an array in the JSON data but got " + data['checkGroups']);
            }
            // validate the optional field `checkGroups` (array)
            for (const item of data['checkGroups']) {
                CheckGroup.validateJSON(item);
            };
        }
        if (data['apps']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['apps'])) {
                throw new Error("Expected the field `apps` to be an array in the JSON data but got " + data['apps']);
            }
            // validate the optional field `apps` (array)
            for (const item of data['apps']) {
                CheckApp.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {module:model/Alert} alert
 */
GetAlerts200Response.prototype['alert'] = undefined;

/**
 * @member {Array.<module:model/Check>} checks
 */
GetAlerts200Response.prototype['checks'] = undefined;

/**
 * @member {Array.<module:model/CheckGroup>} checkGroups
 */
GetAlerts200Response.prototype['checkGroups'] = undefined;

/**
 * @member {Array.<module:model/CheckApp>} apps
 */
GetAlerts200Response.prototype['apps'] = undefined;






export default GetAlerts200Response;

