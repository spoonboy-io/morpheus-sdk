/**
 * Morpheus API
 * Morpheus is a powerful cloud management tool that provides provisioning, monitoring, logging, backups, and application deployment strategies.  This document describes the Morpheus API protocol and the available endpoints. Sections are organized in the same manner as they appear in the Morpheus UI.
 *
 * The version of the OpenAPI document: 6.1.1
 * Contact: dev@morpheusdata.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AddAlertsRequestAlertContactsInner from './AddAlertsRequestAlertContactsInner';

/**
 * The AddAlertsRequestAlert model module.
 * @module model/AddAlertsRequestAlert
 * @version 6.1.1
 */
class AddAlertsRequestAlert {
    /**
     * Constructs a new <code>AddAlertsRequestAlert</code>.
     * Payload for creating a new monitoring alert
     * @alias module:model/AddAlertsRequestAlert
     * @param name {String} Unique name scoped to your account for the alert
     */
    constructor(name) { 
        
        AddAlertsRequestAlert.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>AddAlertsRequestAlert</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AddAlertsRequestAlert} obj Optional instance to populate.
     * @return {module:model/AddAlertsRequestAlert} The populated <code>AddAlertsRequestAlert</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AddAlertsRequestAlert();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('minDuration')) {
                obj['minDuration'] = ApiClient.convertToType(data['minDuration'], 'Number');
            }
            if (data.hasOwnProperty('minSeverity')) {
                obj['minSeverity'] = ApiClient.convertToType(data['minSeverity'], 'String');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('allChecks')) {
                obj['allChecks'] = ApiClient.convertToType(data['allChecks'], 'Boolean');
            }
            if (data.hasOwnProperty('allGroups')) {
                obj['allGroups'] = ApiClient.convertToType(data['allGroups'], 'Boolean');
            }
            if (data.hasOwnProperty('allApps')) {
                obj['allApps'] = ApiClient.convertToType(data['allApps'], 'Boolean');
            }
            if (data.hasOwnProperty('checks')) {
                obj['checks'] = ApiClient.convertToType(data['checks'], ['Number']);
            }
            if (data.hasOwnProperty('groups')) {
                obj['groups'] = ApiClient.convertToType(data['groups'], ['Number']);
            }
            if (data.hasOwnProperty('apps')) {
                obj['apps'] = ApiClient.convertToType(data['apps'], ['Number']);
            }
            if (data.hasOwnProperty('contacts')) {
                obj['contacts'] = ApiClient.convertToType(data['contacts'], [AddAlertsRequestAlertContactsInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AddAlertsRequestAlert</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AddAlertsRequestAlert</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AddAlertsRequestAlert.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['minSeverity'] && !(typeof data['minSeverity'] === 'string' || data['minSeverity'] instanceof String)) {
            throw new Error("Expected the field `minSeverity` to be a primitive type in the JSON string but got " + data['minSeverity']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['checks'])) {
            throw new Error("Expected the field `checks` to be an array in the JSON data but got " + data['checks']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['groups'])) {
            throw new Error("Expected the field `groups` to be an array in the JSON data but got " + data['groups']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['apps'])) {
            throw new Error("Expected the field `apps` to be an array in the JSON data but got " + data['apps']);
        }
        if (data['contacts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['contacts'])) {
                throw new Error("Expected the field `contacts` to be an array in the JSON data but got " + data['contacts']);
            }
            // validate the optional field `contacts` (array)
            for (const item of data['contacts']) {
                AddAlertsRequestAlertContactsInner.validateJSON(item);
            };
        }

        return true;
    }


}

AddAlertsRequestAlert.RequiredProperties = ["name"];

/**
 * Unique name scoped to your account for the alert
 * @member {String} name
 */
AddAlertsRequestAlert.prototype['name'] = undefined;

/**
 * Duration in minutes of the delay before sending notification(s)
 * @member {Number} minDuration
 * @default 0
 */
AddAlertsRequestAlert.prototype['minDuration'] = 0;

/**
 * Severity level threshold for sending notifications.
 * @member {module:model/AddAlertsRequestAlert.MinSeverityEnum} minSeverity
 * @default 'critical'
 */
AddAlertsRequestAlert.prototype['minSeverity'] = 'critical';

/**
 * Set to false to disable notifications
 * @member {Boolean} active
 * @default true
 */
AddAlertsRequestAlert.prototype['active'] = true;

/**
 * Trigger for all checks
 * @member {Boolean} allChecks
 * @default false
 */
AddAlertsRequestAlert.prototype['allChecks'] = false;

/**
 * Trigger for all check groups
 * @member {Boolean} allGroups
 * @default false
 */
AddAlertsRequestAlert.prototype['allGroups'] = false;

/**
 * Trigger for all monitor apps
 * @member {Boolean} allApps
 * @default false
 */
AddAlertsRequestAlert.prototype['allApps'] = false;

/**
 * @member {Array.<Number>} checks
 */
AddAlertsRequestAlert.prototype['checks'] = undefined;

/**
 * @member {Array.<Number>} groups
 */
AddAlertsRequestAlert.prototype['groups'] = undefined;

/**
 * @member {Array.<Number>} apps
 */
AddAlertsRequestAlert.prototype['apps'] = undefined;

/**
 * @member {Array.<module:model/AddAlertsRequestAlertContactsInner>} contacts
 */
AddAlertsRequestAlert.prototype['contacts'] = undefined;





/**
 * Allowed values for the <code>minSeverity</code> property.
 * @enum {String}
 * @readonly
 */
AddAlertsRequestAlert['MinSeverityEnum'] = {

    /**
     * value: "info"
     * @const
     */
    "info": "info",

    /**
     * value: "warning"
     * @const
     */
    "warning": "warning",

    /**
     * value: "critical"
     * @const
     */
    "critical": "critical"
};



export default AddAlertsRequestAlert;

